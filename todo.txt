//todo
* update yeti compile to not use yeti task but command line
* add project dependency
* yetidoc fails when application on path (remove hack regarding ybuilder.yeti)
* add yebspec support
* single test method
* create webapp

//done
+ doc targets
+ yeti doc target
+ AntClassPath for loading buildlibs
+ update retrieve to not duplicate jars in differnt runtime dirs
+ add buildLibs loading to new Project
+ update scopes to have hard coded names
+ test targets
+ change path to be dynamic
+ add arguments to target invoke
+ add properties support
+ jar target
+ war target 
+ jetty support
+ handel ant exceptions better
+ only compile java/yeti dirs if they exist
+ maven target
+ ant exception handling also to creation of tasks
+ update livecycles
+ unifyhelp
+ update dependencies to include only props
+ continues compilation
+ extensiontest should run tests
+ add .gitignore to newProject, update project.yeti and dependencies.yeti
